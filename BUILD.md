# 📦 Сборка Solar System Simulator

Проект готов к упаковке в автономный исполняемый файл!

## Быстрый старт

### Linux / macOS
```bash
chmod +x build.sh
./build.sh
```

### Windows
```cmd
build.bat
```

## Результат сборки
Исполняемый файл будет создан в папке `dist/`:
- **Linux/macOS:** `dist/SolarSystemSimulator`
- **Windows:** `dist/SolarSystemSimulator.exe`

## Распространение
1. Скопируйте файл из папки `dist/` 
2. Отправьте пользователю
3. Пользователь запускает файл — Python не требуется!

---

## Ручная сборка (без скрипта)

### Установка PyInstaller
```bash
pip install pyinstaller
```

### Сборка
```bash
pyinstaller build_config.spec
```

---

## Что входит в сборку
- ✅ Python интерпретатор
- ✅ Pygame 2.5.0
- ✅ Все исходные файлы проекта
- ✅ Конфиг settings.json

## Размер
Примерный размер исполняемого файла: **50-100 МБ**

## Проблемы и решения

### Ошибка: "PyInstaller не найден"
```bash
pip install pyinstaller --upgrade
```

### Ошибка при запуске: "settings.json не найден"
Убедитесь, что `settings.json` находится в корне проекта

### Исполняемый файл медленно запускается
Это нормально — первый запуск распаковывает файлы. Последующие запуски будут быстрее.

---

## Дополнительные опции сборки

Чтобы добавить иконку:
```bash
pyinstaller build_config.spec --icon=icon.ico
```

Чтобы скрыть консоль на Windows:
- В файле `build_config.spec` уже установлено `console=False`

Для минимизации размера:
```bash
pyinstaller --onefile --optimize=2 --strip main.py
```
